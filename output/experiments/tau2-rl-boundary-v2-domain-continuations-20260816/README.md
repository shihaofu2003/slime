# tau2-rl-boundary-v2-domain-continuations-20260816

Purpose: record an aborted submission that used the wrong historical lineage.
No experiment result was produced.

## Configuration

The submitted scripts used boundary-v2, the v1 User SFT, multi-call protocol,
and training `max_steps=60`. The intended configuration was single-call,
raw User, `max_steps=120`, and turn-credit-v1, so the jobs were cancelled.

## Evaluation

No conversion or evaluation is scheduled for this cancelled experiment.

## Jobs

- Mixed job `2727` was stopped before training.
- Retail job `2729` and Telecom job `2730` were cancelled while queued.
- Airline was rejected before a job ID was created.
